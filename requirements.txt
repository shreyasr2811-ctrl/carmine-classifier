# Pinned for reproducible Streamlit Community Cloud builds.
#
# scikit-learn is pinned exactly: models/*.joblib and rag_kb/tfidf_vectorizer.joblib
# were fitted under 1.8.0, and unpickling them under a different minor version
# raises InconsistentVersionWarning and is not guaranteed to be numerically safe.
#
# chromadb is pinned exactly: rag_kb/chroma_store/ is a persisted store written
# by 1.5.9. An older client cannot read a newer store's schema.
streamlit>=1.36,<2
numpy>=1.26,<3
pandas>=2.2
plotly>=5.20
scipy>=1.11
scikit-learn==1.8.0
scikit-image>=0.22
xgboost>=2.0,<4
Pillow>=10
joblib>=1.3
chromadb==1.5.9
langchain>=1.0,<2
langchain-anthropic>=1.0,<2
anthropic>=0.60
python-dotenv>=1.0
